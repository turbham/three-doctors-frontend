mutation Mutation($input: UpdateCartInput!) {
  updateCart(input: $input) {
    items {
      quantity
      product {
        _id
        name
        images
        inventory {
          quantity
        }
        price
        colors
        sizes
        description
        quantity
      }
      _id
    }
    _id
  }
}